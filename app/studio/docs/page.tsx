import Link from "next/link"; import { StudioNav } from "../StudioNav";
export const metadata = { title: "Studio documentation" };
export default function StudioDocs(){return <><StudioNav/><section className="page-hero compact shell"><span className="kicker">Version-matched and offline</span><h1>Studio documentation</h1><p>From first launch to a signed release, with the Edition 5 reference embedded beside your exact toolchain.</p></section><section className="content-shell shell studio-doc-grid">
{[["01","Start here","Detect or install the Nivren toolchain, choose theme and keymap, open a folder, run the first check, and keep metrics off unless you opt in."],["02","Understand code","Navigate symbols, inspect typed failures and capabilities, compare optimized intent, and see why a plan allocates or blocks."],["03","Run and debug","Use tasks, tests, deterministic-time tools, coverage, benchmarks, profiles, breakpoints, watches, and structured-task views."],["04","Work with Agent","Choose a provider, preview the exact redacted context receipt, approve scoped authority, review isolated diffs, and apply or reject."],["05","Remote and containers","Verify an SSH host, review helper capabilities, declare mounts and ports, reconnect safely, and rebuild with a generation receipt."],["06","Package and prove","Build notarized PKG/DMG, timestamped MSI/MSIX, or signed AppImage/DEB/RPM artifacts. Verify pinned signatures, checksums, SPDX SBOMs, provenance, packaged startup, updates, exact rollback, clean installation, safe uninstall, and the complete same-source six-platform matrix before shipping." ]].map(([n,h,p])=><article key={n}><span>{n}</span><h2>{h}</h2><p>{p}</p></article>)}</section><section className="shell studio-callout"><div><span className="kicker">Keyboard-first</span><h2>Everything has a command.</h2><p>Open the palette for commands, symbols, files, tasks, settings, intent views, Agent Space, or release actions. Every binding can be remapped.</p></div><Link className="button primary" href="/docs">Read Edition 5 docs</Link></section></>}
