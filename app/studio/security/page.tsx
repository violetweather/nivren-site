import { StudioNav } from "../StudioNav";
export const metadata={title:"Studio security"};
export default function Security(){return <><StudioNav/><section className="page-hero compact shell"><span className="kicker">Capability controlled</span><h1>Security</h1><p>Workspace content, tools, Agents, plugins, remote helpers, update metadata, and terminal output are untrusted inputs.</p></section><section className="content-shell shell studio-policy"><h2>Process boundaries</h2><p>The Nivren core, Agent worker, plugin host, and remote helper use cancellable length-prefixed protocols capped at 16 MiB. Malformed, replayed, reordered, oversized, timed-out, or unauthorized messages fail closed.</p><h2>Updates and recovery</h2><p>Artifacts are channel, version, platform, size, hash, and signature bound. Activation is atomic, generations only increase, and the prior verified artifact remains available for rollback.</p><h2>Workspace integrity</h2><p>Traversal, symlinks, device files, stale writes, terminal escapes, implicit Git authority, and undeclared external effects are rejected or reduced to inert reviewable data.</p><h2>Reporting</h2><p>No critical or high-severity internally identified defect may remain at 1.0. An independent audit is post-1.0 hardening and will not be claimed before it occurs.</p></section></>}
