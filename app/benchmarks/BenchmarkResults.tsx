import report from "@/benchmarks/nivren-vs-node/results/2026-08-31-phase-n.json";
const format=(ms:number)=>`${ms<10?ms.toFixed(2):ms.toFixed(1)} ms`;
export function BenchmarkResults(){return <><section className="page-hero compact"><div className="shell"><span className="kicker">Measured on August 31, 2026</span><h1>Performance, in context.</h1><p>Twelve workloads. Reproducible results. A clear view of where Nivren is faster and where there’s work to do.</p></div></section><div className="shell content-shell benchmark-page"><div className="benchmark-context"><div><strong>Machine</strong><span>{report.environment.cpu.trim()}</span></div><div><strong>Runtimes recorded</strong><span>{report.environment.nivren}<br/>Node.js {report.environment.node}</span></div><div><strong>Measurement</strong><span>Median wall-clock time<br/>Lower is better</span></div></div><div className="benchmark-table-wrap" tabIndex={0} aria-label="Scrollable benchmark results"><table className="benchmark-table"><caption className="sr-only">Nivren and Node.js median latency for all twelve published workloads</caption><thead><tr><th scope="col">Workload</th><th scope="col">Nivren</th><th scope="col">Node.js</th><th scope="col">Faster runtime</th></tr></thead><tbody>{report.results.map(row=><tr key={row.id}><th scope="row"><strong>{row.label}</strong><small>{row.description}</small></th><td>{format(row.nivren.median_ms)}</td><td>{format(row.node.median_ms)}</td><td className={row.nivren.median_ms<row.node.median_ms?"nivren-win":"node-win"}>{row.nivren.median_ms<row.node.median_ms?"Nivren":"Node.js"}<small>{(Math.max(row.nivren.median_ms,row.node.median_ms)/Math.min(row.nivren.median_ms,row.node.median_ms)).toFixed(2)}×</small></td></tr>)}</tbody></table></div><section className="benchmark-method"><h2>What these numbers mean.</h2><p>These are specific workloads on one machine, not an overall language ranking. The recorded runtime version is shown above exactly as it appears in the source report.</p><p>Fresh-process tests include startup and source processing. The HTTP workload measures a warmed, long-running service. Paired programs produce identical required output; the source-checking test compares each runtime’s built-in checker.</p><a className="text-link" href="https://github.com/violetweather/nivren-site/tree/main/benchmarks/nivren-vs-node">Inspect the harness and complete results <span aria-hidden="true">↗</span></a></section></div></>;}
