import Link from "next/link";
import {packages} from "./catalog";
import {SyntaxCode} from "../components/SyntaxCode";
export function PackageDirectory(){return <><section className="page-hero compact"><div className="shell"><span className="kicker">The official collection</span><h1>Small packages.<br/>Useful possibilities.</h1><p>Find the right piece for your application. Every package includes API documentation, examples, and clear permission requirements.</p><div className="page-hero-meta"><span className="meta-pill">{packages.length} official packages</span><span className="meta-pill">Version 1.0.0</span><span className="meta-pill">Signed registry</span></div></div></section><div className="shell content-shell package-index"><div className="package-grid">{packages.map(item=><article className="package-card" key={item.name}><span>{item.purpose}</span><h2>{item.name}</h2><p>{item.summary}</p><Link href={`/packages/${item.name}`}>Read the guide <span aria-hidden="true">↗</span></Link></article>)}</div><section className="verify-section"><div><h2>Verified on your machine.</h2><p>The <a href="https://violetweather.github.io/nivren-registry">signed registry</a> provides immutable package archives. Your client checks each release against a pinned Ed25519 root key before installing it.</p><Link className="text-link" href="/docs">Learn about package trust <span aria-hidden="true">↗</span></Link></div><div className="prose-card"><h3>Add a package</h3><pre tabIndex={0}><SyntaxCode language="shell" code={`niv add nivren_stats 1.0.0
niv install --trusted \\
  https://violetweather.github.io/nivren-registry \\
  ./nivren-root.pub`}/></pre><p>Obtain and verify the registry’s root key before trusting it.</p></div></section></div></>;}
