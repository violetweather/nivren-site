define main takes { } gives Result<Nothing, Problem> needs FileRead {
    keep source set std.files.read("data/events.log") or give
    keep lines set std.text.split(source, "
", 1000) or give
    keep encoded set std.json.encode(lines) or give
    show(encoded)
    give ok(none)
}

main()
