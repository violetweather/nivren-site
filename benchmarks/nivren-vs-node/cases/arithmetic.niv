define kernel takes { value is Int, salt is Int } gives Int {
    give (value + salt) * 2
}

change index set 0
change result set 0
repeat index < 50000000 {
    result = kernel(index, 3)
    index = index + 1
}
show(result)
